# -----------------------
# Core / language-specific
# -----------------------

# Byte-compiled / optimized / cache files
__pycache__/
*.py[cod]
*$py.class

# C extensions build artifacts (if any are built)
*.so
*.dylib
*.dll

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg
pip-wheel-metadata/
*.whl

# Virtual environments
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/

# Python environment vars / secrets
.env          # local environment overrides (contains API keys, secrets)
*.env.local
*.env.*.local

# -----------------------
# Project-specific data
# -----------------------

# Vector stores / embedding indexes (large, regenerable)
# Adjust if you intentionally want to commit a prebuilt index.
vector_store/
index/
embeddings/
*.db              # e.g., sqlite used for caching or local storage (if ephemeral)
*.sqlite

# Cached answers / intermediate artifacts
cache/
*.cache/

# Logs and runtime state
logs/
*.log
*.pid
*.sock

# Temporary files created during development or query simulation
tmp/
temp/

# -----------------------
# Operating system
# -----------------------

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux swap / runtime
*~                # editor temporary
.nfs*              # NFS temp files

# -----------------------
# Editor / IDE
# -----------------------

# VSCode
.vscode/
.history/
*.code-workspace

# JetBrains (PyCharm, IntelliJ)
.idea/
*.iml
*.ipr
*.iws

# Sublime
*.sublime-project
*.sublime-workspace

# Emacs
*~ 
\#*\#
.cache/
auto-save-list/

# Vim
*.swp
*.swo
*.un~
Session.vim

# -----------------------
# Dependency / lock / build tooling
# -----------------------

# pip cache (optional; usually global)
pip-log.txt
pip-delete-this-directory.txt

# Poetry
poetry.lock        # include only if you want reproducible locked deps; otherwise ignore

# -----------------------
# Third-party / external
# -----------------------

# OpenAI credentials / local model dumps (if any)
openai_credentials.json
local_models/      # if downloading large local embeddings/LLM weights (ignore if you intentionally check in small ones)

# UI build artifacts (if you ever add a build step)
node_modules/
dist-ui/

# -----------------------
# Secrets & sensitive
# -----------------------

# Generic secret files you might accidentally create
*.key
*.pem
*.cert
credentials.json
secrets.yaml

# -----------------------
# Optional / recommendations
# -----------------------

# If your corpus is large and you don't want to version every text file, uncomment:
# /corpus/
# /lore_texts/

# If you commit sample or minimal corpus, keep those directories tracked and put large raw dumps elsewhere.

# -----------------------
# Git tooling
# -----------------------

# Git metadata that can get generated by some helpers (safe to ignore)
*.orig

# -----------------------
# End of .gitignore
# -----------------------
